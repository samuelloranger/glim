name: Publish docs to example.com

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "install.sh"
      - ".github/workflows/docs-pages.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: docs-publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - name: Build docs
        env:
          GITHUB_ACTIONS: "true"
        run: bun run docs:build
      - name: Push built docs to samlo-cloud
        env:
          TOKEN: ${{ secrets.DOCS_TOKEN }}
        run: |
          git clone --depth 1 "https://x-access-token:${TOKEN}@github.com/samuelloranger/samlo-cloud.git" out
          rm -rf out/docs/glim
          mkdir -p out/docs/glim
          cp -r docs/.vitepress/dist/. out/docs/glim/
          cd out
          git config user.name  "github-actions"
          git config user.email "github-actions@example.com"
          git add docs/glim
          git diff --cached --quiet || git commit -m "docs(glim): sync ${GITHUB_SHA::7}"
          git push
